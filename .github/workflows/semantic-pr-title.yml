name: Semantic PR Title

on:
  pull_request_target:
    types: 
      - opened
      - edited
      - reopened
      - synchronize

jobs:
  lint-pr-title:
    uses: ./.github/workflows/reusable-semantic-pr-title.yaml
    permissions:
      pull-requests: write
      statuses: write
